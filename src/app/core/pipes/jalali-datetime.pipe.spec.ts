import { JalaliDatetimePipe } from './jalali-datetime.pipe';

describe('JalaliDatetimePipe', () => {
  it('create an instance', () => {
    const pipe = new JalaliDatetimePipe();
    expect(pipe).toBeTruthy();
  });
});
