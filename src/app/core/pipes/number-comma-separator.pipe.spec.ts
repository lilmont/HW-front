import { NumberCommaSeparatorPipe } from './number-comma-separator.pipe';

describe('NumberCommaSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberCommaSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
