import { NormalizePhoneDirective } from './normalize-phone.directive';

describe('NormalizePhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new NormalizePhoneDirective();
    expect(directive).toBeTruthy();
  });
});
